import type { Meta } from '@storybook/react';
import { FileDetail } from './FileDetail';

const Story: Meta<typeof FileDetail> = {
  component: FileDetail,
  title: 'FileDetail',
};
export default Story;

export const Primary = {
  args: {},
};
